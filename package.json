{
  "name": "suknna-blog",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build && node scripts/postbuild.mjs",
    "preview": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "vitepress": "^1.2.2"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "wrangler": "^4.69.0"
  },
  "engines": {
    "node": ">=18"
  }
}
