---
summary: "这是一个示例文章。你可以删除它，然后把自己的 Markdown 放进 /note。"
---

# Hello World

把任意 `*.md` 放在 `note/` 下（建议按目录做分类），push 到 GitHub 后会自动触发构建并部署。

你也可以在 frontmatter 里写：

```yaml
---
title: "自定义标题（可选）"
category: "自定义分类（可选）"
summary: "列表页摘要（可选）"
---
```
